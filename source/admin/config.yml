backend:
  name: github
  repo: gen035/portfolio # Path to your GitHub repository
  branch: master # Branch to update (master by default)

media_folder: "images" # Folder where user uploaded files should go